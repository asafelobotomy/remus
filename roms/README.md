# ROM Storage

Use this folder as the canonical local home for repository-scoped ROM input.

## Rules

- Place local ROMs and disc images under `roms/`.
- Organize by system when useful.
- Do not commit ROM payloads.
- Do not maintain an inventory of ROM contents in git.

This file exists so the folder is visible in the repository without exposing ROM data.