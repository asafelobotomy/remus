# Commit Style
Use Conventional Commits with a concise subject line and an optional body when context is needed.
Format:
- type(scope): subject

Examples:
- feat(core): add hash-first verification fallback
- fix(cli): guard missing database path in verify command
- refactor(metadata): extract thumbnail url helper
